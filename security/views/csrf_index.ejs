<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Google</title>
  <meta name="description" content="Sample site">
  <meta name="author" content="phadyniak">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
</head>

<body>
	<div class='container'>
		<div class='page-header'>
			<h1>Google</h1>
		</div>
		<div class='row'>
			<div class='col-md-4'>
				<% if (!user) { %>
				<form action='/login' method='POST'>
					<label>Username:</label>
					<input type='text' name='username' class='form-control' value='user' />
					<label>Password:</label>
					<input type='password' name='password' class='form-control' value='' /><br/>
					<button type='submit' class='btn btn-primary'>Login</button>
				</form>
				<% } else { %>
					<p>
						Hello, <b></b><%= user.username %></b>
					</p>
					<p>
						<a href='/logout'>logout</a>
					</p>
					<h4>Messages</h4>
					<form action='/add_message' method="POST">
						<input name='message' class='form-control' /><br/>
						<button type='submit' class='btn btn-primary'>Send</button>
					</form>
					<br />
					<div>
						<% messages.forEach(message => {%>
							<div>
								<b><%= message.author %></b>: <span><%- message.text %></span>
							</div>
						<% }) %>
					</div>
				<% } %>
			</div>
			<div class='col-md-8'>
				<p>
                    Bla-bla-bla.
                </p>
                <a href='/sql'>SQL Injection Sample</a>
			</div>
		</div>
	</div>
</body>
</html>
